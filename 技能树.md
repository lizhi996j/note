- 编程技能：
    - 读文档技能（说明书）: 
        - 查看vscode文档和matplotlib文档;
        - 找AI读文档；
    - 编程不过是掉API;

- vscode介绍：
    - vscode界面 (看一下文档，知道这些名字才方便debug)
        - activation bar
        - primary bar
        - edtitor
        - explorer
    - vscode命令:
    - vscode setting:
    - vscode 插件： python插件； 安装jupyter notebook;
    - 查看vscode文档：还有更多组成部分，比如SSH连接

- 用命令行使用anaconda：
    - 为什么用不同环境？ 任务间隔离，每篇论文每个任务用各自工具；
    - conda 命令：
        - 创建环境
        - 激活环境
        - 删除环境
        - 迁移环境

- python基础：
    - python语法比较困难的点：
        - 查看环境路径，包路径，文件路径；
        - 函数参数类型；
    - 需要学习的python包：
        - pandas：R语言中表格的实现，二维数据；
        - numpy: 高维数据；
        - matplotlib: 画图工具，论文中的图都是用这种画的；
        - pytorch: 神经网络包；(cuda安装->pytorch安装)
        - sklearn: linear,svm，tree等模型包


- 神经网络与大模型基础：
    - 训练核心：计算图，维度变化， 梯度操作
    - 语言模型和Seq2Seq模型（Transformer)；
    - 大模型： transformer decoder only 而已；
    - 大模型基础知识框架：
        - 预训练+微调+人类反馈强化学习
        - 微调方法： 最著名的LoRA;
        - prompt工程： chain of thought;
        - RAG: 向量检索器；
        - Agent: 规划+调用工具；

- cursor与copilot:
    tab功能：
    ask功能：
    agent功能：
    

- 翻墙：
    - 虚拟专网(vpn)
    - 代理(proxy)+机场


- 后记：
    - 实现是繁琐的，但概念是容易理解的， 理解概念之后就可以讨论了；